# Deep Audit Findings — 2026-07-07

Produced by a 14-agent audit (9 bug-finder dimensions + 5 goal-gap analyses) over the full codebase, deduplicated. The adversarial verification stage was interrupted by an API session limit, so items below are finder reports; those marked **[FIXED]** were manually verified against the code and fixed the same day (see git history). Treat unmarked items as high-quality leads to confirm before fixing.


## CRITICAL

### Failing unit test: 'imports XLSX workbook data and resets the file picker' fails deterministically
- **Where:** `src/App.test.tsx:813` · test-ground-truth
- **Failure:** Run `corepack pnpm vitest run` -> 'FAIL src/App.test.tsx > App > imports XLSX workbook data and resets the file picker; TestingLibraryElementError: Unable to find role="gridcell" and name "A1 Project"; src/App.test.tsx:833:25; Test Files 1 failed | 18 passed (19); Tests 1 failed | 247 passed (248)'. Deterministic: also fails in isolation with `vitest run src/App.test.tsx -t "imports XLSX workbook data"`. Status bar in failure DOM shows 'Ready' at the 1s dump, and 'Imported budget.xlsx' at ~3.5s.
- **Fix sketch:** Pass an explicit timeout to the assertion, e.g. `await screen.findByRole("gridcell", { name: "A1 Project" }, { timeout: 10000 })`, or configure testing-library asyncUtilTimeout globally. Separately, investigate why a 2-cell workbook takes 3.5s to import+render (engine rebuild + full-grid render).

### RangeError crash: populated-cell coords spread into Math.min/Math.max on large sheets **[FIXED]**
- **Where:** `src/App.tsx:2366` · correctness
- **Failure:** Import a 130k-row x 1-col (or 13k x 10) dataset; on the next render with a single-cell selection (the default), Math.min(...) throws RangeError inside useMemo, React unmounts the tree, entire app crashes. Directly contradicts the 'huge datasets without hanging' goal.
- **Fix sketch:** Replace spreads with a single reduce/for-loop over Object.keys(sheet.cells), and only compute pivotSourceRows when the pivot panel is open.

### Pivot source rows materialize the entire populated sheet through HyperFormula on every selection change, even with panel closed **[FIXED]**
- **Where:** `src/App.tsx:201` · performance
- **Failure:** User loads a 100k-row dataset and presses an arrow key: each keystroke synchronously rebuilds a 2M-cell string matrix inside render; UI freezes for seconds per keystroke — an effective hang on the exact 'huge dataset' workloads the app targets.
- **Fix sketch:** Compute source rows lazily: only when isPivotPanelOpen, and/or snapshot headers once when the panel opens; never expand a single-cell selection during passive rendering.

### Every workbook mutation rebuilds the entire HyperFormula engine from a dense rowCount x columnCount matrix **[FIXED]**
- **Where:** `src/App.tsx:191` · performance
- **Failure:** Import a 100k-row xlsx (xlsx.ts sets rowCount = maxRow+1), type '5' into any cell and press Enter -> sheetToMatrix allocates 100k row arrays / 2.6M address strings, HyperFormula re-parses 2.6M cells and re-evaluates all formulas -> multi-second full tab freeze on EVERY single edit; with 2-3 such sheets, memory spikes can OOM the tab.
- **Fix sketch:** Keep one long-lived HyperFormula instance; on edit call hyperFormula.setCellContents for just the changed addresses (and addRows/removeRows for structure ops). Build initial sheets from sparse cell maps (buildFromSheets accepts ragged arrays; only materialize up to last populated row/column per row).

### pivotSourceRows recomputes on every selection change, scanning the entire populated extent through HyperFormula even when the pivot panel is closed **[FIXED]**
- **Where:** `src/App.tsx:201` · performance
- **Failure:** Open a 100k-row x 26-col imported sheet and press ArrowDown: each keypress triggers 2.6M HyperFormula getCellValue calls + 2.6M string allocations -> tab hangs for many seconds per keystroke. Additionally, with more than ~125k populated cells, Math.min(...) / Math.max(...) spread throws RangeError: Maximum call stack size exceeded inside the useMemo, crashing the whole app to a white screen during render.
- **Fix sketch:** Compute source rows lazily only when the pivot panel opens (or on Create), gate the memo on isPivotPanelOpen, and replace Math.min(...spread) with a reduce loop. Track the populated bounding box incrementally in the sheet model instead of rescanning.

### Whole-column/whole-sheet selection triggers O(selection) HyperFormula scan and Math.min/max spread crash in summarizeSelection **[FIXED]**
- **Where:** `src/App.tsx:2393` · performance
- **Failure:** With 100k rows, click column A's header: 100k address strings + 100k HyperFormula getCellValue calls + Number() parses run synchronously -> multi-second freeze. If the column has >~125k numeric values (or the corner button selects a 2.6M-cell sheet), the Math.min spread throws RangeError and crashes the app during render. Clicking the corner cell on 100k x 26 = 2.6M cells is a guaranteed hang.
- **Fix sketch:** Iterate the range with a loop (no address array), cap the summary at the populated bounding box intersected with the selection, track min/max/sum in a single pass, and consider sampling/deferring to idle time for very large selections.

### StatusBar selectedCount materializes an address string for every selected cell on every App render **[FIXED]**
- **Where:** `src/App.tsx:2098` · performance
- **Failure:** Select all via the corner cell on a 100k x 26 sheet: every subsequent render allocates 2.6M strings (~100+ MB churn); combined with summarizeSelection this hangs the tab immediately on selection and re-hangs on every re-render. Even a single 100k-row column selection costs 100k allocations per render while typing.
- **Fix sketch:** Compute count arithmetically: (end.row-start.row+1)*(end.column-start.column+1) from the normalized range.

### Full-workbook JSON.stringify to localStorage on every commit; quota overflow throws uncaught in effect and crashes the app **[FIXED]**
- **Where:** `src/App.tsx:216` · data-loss
- **Failure:** Import a 60MB-equivalent 100k-row CSV: the import commit triggers JSON.stringify (multi-second freeze) then setItem throws QuotaExceededError -> unhandled error in effect -> React unmounts the app -> white screen, imported data and all prior work lost. Below the quota, every keystroke commit still pays a full-workbook stringify (hundreds of ms at scale).
- **Fix sketch:** Wrap setItem in try/catch and surface a status warning; debounce saves (e.g. 1-2s idle); serialize incrementally or move to IndexedDB (async, larger quota) for big workbooks.

### Stale cut-clipboard survives sheet deletion/workbook replacement: paste crashes the app or silently deletes data
- **Where:** `src/App.tsx:1164` · data-loss
- **Failure:** Cut A1:B5 on Sheet2, delete Sheet2, press Ctrl+V (or context-menu Paste) on Sheet1 -> getSheet throws -> app white-screens. Alternatively: cut a range, Import XLSX, Ctrl+V -> stale content pastes AND the corresponding range on the imported file's sheet with the same id is silently erased.
- **Fix sketch:** Clear richClipboard in handleDeleteSheet/handleNewWorkbook/handleImportCsv/handleImportXlsx (or validate sourceSheetId still exists before move-paste and downgrade to copy-paste); use non-reusable ids (crypto.randomUUID).

### Render crash on large sheets: inferSourceRange spreads every populated cell coordinate into Math.min/Math.max **[FIXED]**
- **Where:** `src/App.tsx:2366` · correctness
- **Failure:** Import a dataset with ~150k populated cells (well within the 'huge datasets' goal), click any single cell (or just load the saved workbook) -> RangeError during render -> blank page.
- **Fix sketch:** Compute bounds with a single reduce loop (like activeSheetToRows does at 2167) instead of spread; only compute pivotSourceRows when isPivotPanelOpen.

### Whole-sheet and whole-selection derived state recomputed on every selection move even with panels closed (UI hangs) **[FIXED]**
- **Where:** `src/App.tsx:201` · performance
- **Failure:** On a 100k-row imported sheet, every arrow-key press rebuilds a 100k-row display matrix and rescans all formulas (multi-second freeze per keystroke); selecting a full column or Ctrl+A on numeric data hangs the tab or crashes via the spread RangeError.
- **Fix sketch:** Gate pivotSourceRows/formulaAuditDependents behind their panels' open flags; compute selection count arithmetically ((rows)*(cols)); cap/stream summarizeSelection with a loop and an upper bound.

### HyperFormula engine fully rebuilt from a dense matrix on every commit; old instances never destroyed **[FIXED]**
- **Where:** `src/App.tsx:191` · performance
- **Failure:** Type values into a 200k-row imported sheet: every keystroke commit re-serializes 200k x 26 dense arrays and re-parses all formulas (seconds of main-thread hang per edit), while abandoned HyperFormula instances accumulate until the tab OOMs.
- **Fix sketch:** Keep a single engine instance (useRef) and apply incremental updates via HyperFormula setCellContents/addRows APIs, or at minimum call rebuild()/destroy() and build from sparse data.

### Autosave effect: localStorage.setItem has no error handling and serializes the entire workbook on every commit **[FIXED]**
- **Where:** `src/App.tsx:216` · data-loss
- **Failure:** Import a 30MB XLSX -> import succeeds, then the autosave effect throws QuotaExceededError -> app crashes to a blank page; reloading restores only the last successfully saved (pre-import) state, so the import and any subsequent edits are lost.
- **Fix sketch:** Wrap saveWorkbook in try/catch and surface 'Autosave failed (workbook too large)' in status; debounce saves; move persistence to IndexedDB for large workbooks.

### Every commit rebuilds HyperFormula from a dense rowCount×columnCount matrix — typing/resizing hangs on large sheets **[FIXED]**
- **Where:** `src/App.tsx:191` · performance
- **Failure:** Import a 200k-row xlsx (rowCount becomes maxRow+1). Type '5' into any cell and press Enter: commitCell -> commitWorkbook -> new workbook identity -> dense 200k×26 (5.2M slot) matrix allocation + full HyperFormula rebuild. Every keystroke commit, column resize, or sort freezes the UI for seconds and burns hundreds of MB; the app is unusable at the stated 'huge dataset' goal.
- **Fix sketch:** Keep one HyperFormula instance and apply incremental updates (setCellContents, addRows/removeRows, setSheetContent) on commit; build initial sheets from sparse cell maps instead of dense matrices; only rebuild on import/new-workbook.

### Selecting a whole column/sheet materializes every cell address and evaluates every cell; Math.min/max spread can crash the render **[FIXED]**
- **Where:** `src/App.tsx:2393` · performance
- **Failure:** Import a sheet with 150k numeric rows and click the column-A header (one click): getRangeAddresses builds 150k strings, 150k HyperFormula calls run for the summary, then Math.min(...150k values) throws RangeError during render -> React unmounts to a blank page. Even below the crash threshold, the click freezes the tab for seconds, and every subsequent render recounts the full range.
- **Fix sketch:** Compute count from range arithmetic ((r2-r1+1)*(c2-c1+1)); iterate with loops instead of materializing address arrays; use reduce instead of spread for min/max; cap/lazily compute selection statistics for huge ranges.

### AutoFilter dropdown choices recomputed for every header cell on every Grid render
- **Where:** `src/components/Grid.tsx:607` · performance
- **Failure:** Apply an AutoFilter to a 100k-row x 10-column table (header row visible). Every Grid render — every keystroke while editing, every arrow key, every scroll event — executes 10 x 100k = 1M HyperFormula getCellValue calls plus 10 sorted de-dup passes. The tab hangs permanently as soon as the header row scrolls into view.
- **Fix sketch:** Compute choices only when isAutoFilterMenuOpen for that column (inside the menu-open branch), memoize per (sheet, column, filter state), and cap the distinct-values list like Excel (10k items).

### Row filters re-evaluated through HyperFormula for every row on every Grid render
- **Where:** `src/components/Grid.tsx:114` · performance
- **Failure:** Filter a 100k-row table by one column, then type a character into any cell: each keystroke re-runs 100k HyperFormula getCellValue calls + matchesFilterValue string work, and scroll events repeat it at event rate -> seconds of freeze per keystroke, unusable scrolling.
- **Fix sketch:** Memoize visible rows on [sheet.filters, sheet.id, workbook version] (values only change when the workbook changes), and cache row visibility as a bitset invalidated on edit rather than recomputing per render.

### Grid does O(rowCount) filtering/measurement work on every render — every scroll event and editing keystroke re-walks all rows
- **Where:** `src/components/Grid.tsx:114` · performance
- **Failure:** Sheet with 500k rows: every wheel tick allocates ~3 arrays of 500k entries plus 500k measurement objects and does 4 linear scans -> 100ms+ per scroll frame plus GC pauses (scrolling 'hangs'). Add one AutoFilter on a 100k-row table and type in a cell: each keystroke re-renders Grid and evaluates 100k HyperFormula display values -> multi-second freeze per keystroke.
- **Fix sketch:** Memoize visibleRows/measurements keyed on (sheet, filters, hiddenRows, resizeDraft) with stable identities; use prefix sums + binary search for first/last visible row; cache filter verdicts per row and invalidate on data change; don't re-run filtering on scroll or keystroke renders.

### Conditional-format rules with range-dependent conditions do O(range) or O(range log range) work per visible cell per render
- **Where:** `src/lib/conditionalFormatting.ts:149` · performance
- **Failure:** Apply a 'Top 10' or 'Duplicate values' rule to a 100k-row column (normal Excel usage): each render evaluates ~40 visible rows x O(100k) filter or O(100k log 100k) sort = 10^8-10^9 ops -> multi-second freeze on every keystroke/scroll/selection change. With >~125k values, the Math.max spread in dataBar/colorScale throws RangeError during render and crashes the app.
- **Fix sketch:** Precompute per-rule aggregates once per workbook version (value-count map for duplicate/unique, rank threshold for top/bottom, min/max for colorScale/dataBar) and evaluate each cell in O(1) against them; replace spreads with loops.

### Data-bar/color-scale rules spread per-cell arrays into Math.max -> RangeError; persisted rule bricks the app **[FIXED]**
- **Where:** `src/lib/conditionalFormatting.ts:59` · correctness
- **Failure:** User selects a full column of a 200k-row imported dataset and applies a data bar or color scale. First Grid render throws RangeError during render -> React unmounts -> white screen. Because the rule is persisted to localStorage, every reload crashes again: the app is bricked until the user manually clears site storage.
- **Fix sketch:** Replace spread with a reduce/loop for min/max; compute rule min/max once per rule (cache alongside the values cache) instead of per evaluated cell.

### Any sheet exceeding 40,000 rows crashes the whole app (HyperFormula default maxRows not raised) **[FIXED]**
- **Where:** `src/lib/formulaEngine.ts:51` · correctness
- **Failure:** User pastes a 50,000-row CSV (a stated 'huge dataset' use case) or types a value in cell A40001 via the name box. The next render calls buildFromSheets, SheetSizeLimitExceededError is thrown, React unmounts the tree — blank screen; the offending edit is lost (saveWorkbook effect never runs for the failed render).
- **Fix sketch:** Pass maxRows/maxColumns matching the app's supported grid size (e.g. Excel's 1,048,576/16,384) in the HF config, and/or clamp rowCount growth; wrap engine construction in an error boundary.

### Formula engine is fully rebuilt as a dense rowCount x columnCount matrix on every single edit — O(rows*cols) per keystroke, guaranteed hang on large sheets **[FIXED]**
- **Where:** `src/lib/formulaEngine.ts:72` · performance
- **Failure:** Workbook with 30,000 populated rows across 3 sheets: typing a single character into any cell freezes the UI for multiple seconds (full engine rebuild in render); at larger sizes the tab is effectively hung, defeating the app's core 'huge datasets without hanging' requirement.
- **Fix sketch:** Keep one long-lived HyperFormula instance and mirror workbook mutations through its incremental CRUD API (setCellContents, addRows/removeRows, addSheet/removeSheet/renameSheet, batch()); build initial sheets from sparse content only up to the used range; call destroy() when replacing an instance.

### Engine keeps HyperFormula's 40,000-row default; sheets larger than that crash the whole app (Excel supports 1,048,576 rows) **[FIXED]**
- **Where:** `src/lib/formulaEngine.ts:51` · correctness
- **Failure:** User imports a 50,000-row .xlsx or CSV (xlsx.ts sets rowCount = maxRow+1). commitWorkbook succeeds, the next render calls createFormulaEngine -> HyperFormula.buildFromSheets throws SheetSizeLimitExceededError -> uncaught render exception unmounts the React tree -> blank page, imported data lost. Excel-correct behavior: open the file fully up to 1,048,576 rows.
- **Fix sketch:** Pass maxRows: 1048576, maxColumns: 16384 in the config; additionally wrap engine construction and surface a graceful error, and consider incremental setCellContents updates instead of full rebuilds.

### Untested: importing a CSV/XLSX with >40,000 rows crashes the whole app ('Sheet size limit exceeded') **[FIXED]**
- **Where:** `src/lib/formulaEngine.ts:51` · test-coverage
- **Failure:** User imports a 50,000-row CSV (or opens a 50k-row XLSX). replaceActiveSheetWithRows sets rowCount=50000 -> commitHistory -> re-render -> createFormulaEngine throws 'Sheet size limit exceeded' during render -> React unmounts the app -> blank screen; the imported data is never saved and is lost.
- **Fix sketch:** Pass maxRows/maxColumns (or sheet-size-derived values) to HyperFormula.buildFromSheets; add a regression test that imports >=50k rows and asserts the app stays alive; add an ErrorBoundary and wrap engine construction.

### Unguarded localStorage save: quota overflow crashes app on every edit and stops all persistence **[FIXED]**
- **Where:** `src/lib/persistence.ts:8` · data-loss
- **Failure:** User imports/pastes a large CSV (a few hundred thousand cells easily exceeds 5MB serialized). The next edit fires the effect, setItem throws QuotaExceededError inside useEffect, React unmounts the entire tree -> white screen; all unsaved session work is lost. Even before quota, stringifying a huge workbook on every keystroke freezes typing.
- **Fix sketch:** Wrap setItem in try/catch, surface a 'workbook too large to autosave' status instead of throwing; debounce saves; consider IndexedDB for large workbooks. Add a top-level ErrorBoundary.

### fillDown/fillRight/autoFill/Replace-All are quadratic: full cells-record copy per written cell
- **Where:** `src/lib/workbook.ts:1411` · performance
- **Failure:** On a sheet with 100k populated cells, select a column cell and press Ctrl+D over 10k rows, or drag the AutoFill handle down 10k rows: ~10k iterations x ~100k-entry object spreads (x4 for autofill) = 10^9-10^10 operations -> tab hangs for minutes or is killed. Replace All with 10k matches behaves identically.
- **Fix sketch:** Batch: copy the cells/formats/validations records once per operation, mutate the copies in the loop, and produce one new sheet object (like pasteMatrix already does).

### Row/column insert/delete corrupts formulas via naive regex (function names, numeric literals, string literals, quoted sheet names)
- **Where:** `src/lib/workbook.ts:2310` · correctness
- **Failure:** Sheet has B1 = '=LOG10(A1)' and C1 = '=1E5+A1'. User right-clicks row 1 and inserts a row above. B2 now reads '=LOG11(A2)' evaluating to #NAME?, C2 reads '=1E6+A2' silently off by 900000. A user with '=sum(a1:a10)' who deletes row 3 gets a formula still summing a1:a10 — wrong total with no error.
- **Fix sketch:** Reuse the guarded, quote-aware translation machinery from formulaReferences.ts (transformUnquotedFormulaSegments + lookbehind pattern with i flag and next-char guards) for structural shifts, or better, delegate reference transformation to HyperFormula's own addRows/removeRows/addColumns/removeColumns operations and read back serialized formulas.

### shiftFormulaReferences rewrites formulas with a naive regex, corrupting function names with digits, string literals, and cross-sheet references on row/column insert/delete
- **Where:** `src/lib/workbook.ts:2310` · data-loss
- **Failure:** Sheet has =LOG10(B2) in C1. User inserts a row above row 2. The formula is rewritten to =LOG11(B3) -> #NAME? permanently persisted (undoable only via history). Similarly ="Q1"&A5 becomes ="Q2"&A6 after a column insert. Excel-correct behavior: only real cell references are adjusted; string literals and function names are never touched.
- **Fix sketch:** Reuse translateFormulaReferences' quoted-segment walker and its next-character guards ("!", "(") in shiftFormulaReferences, add /i, or delegate reference transforms to HyperFormula's addRows/removeRows operations.

### Untested: shared formulas from real Excel files import as references to the master cell — silent formula corruption
- **Where:** `src/lib/xlsx.ts:1193` · excel-fidelity
- **Failure:** Open an .xlsx created in Excel where column B was filled by dragging '=A1*2' down 1000 rows: after import, B2:B1000 all read '=B1' and display the same value as B1; totals/downstream formulas are silently wrong; saving/exporting bakes the corruption in.
- **Fix sketch:** Resolve shared formulas by translating the master formula to each slave address (ExcelJS provides master ref; reuse translateFormula from formulaReferences.ts), and add an import test built with worksheet.fillFormula(..., 'shared') as a real-Excel fixture.


## HIGH

### Pivot is a dead one-shot snapshot: no refresh, config discarded, silently stale after source edits
- **Where:** `src/App.tsx:1103` · excel-fidelity
- **Failure:** User builds 'Pivot 1' over sales data, then corrects a mistyped amount in the source sheet; Pivot 1 keeps the old totals indefinitely; user ships numbers from the stale sheet believing pivots update like Excel's (which refresh on demand/on open).
- **Fix sketch:** Persist {config, sourceSheetId, sourceRange, targetSheetId} on the workbook; add a Refresh action (and refresh-on-activate) that recomputes and rewrites the pivot block.

### CSV import fails for large files: column count computed via argument spread over all rows
- **Where:** `src/App.tsx:2122` · correctness
- **Failure:** Import a 200k-row CSV -> import aborts, status bar shows 'Maximum call stack size exceeded', no data loaded — directly defeating the huge-dataset goal.
- **Fix sketch:** rows.reduce((max, row) => Math.max(max, row.length), 26).

### 'New workbook' irreversibly destroys the current workbook with no confirmation and no undo
- **Where:** `src/App.tsx:1500` · data-loss
- **Failure:** One misclick on File -> New workbook: the entire workbook (all sheets) is gone; Ctrl+Z does nothing; reload restores the already-overwritten blank state.
- **Fix sketch:** Route the blank workbook through commitHistory (undoable) and/or require window.confirm before discarding unsaved data.

### CSV export writes raw formula text instead of computed values
- **Where:** `src/App.tsx:1527` · excel-fidelity
- **Failure:** A sheet with a totals column of formulas exported to CSV yields '=SUM(...)' strings; any downstream consumer (pandas, another spreadsheet without recalculation context) reads garbage instead of numbers.
- **Fix sketch:** Build export rows with getVisibleCellText/formulaEngine.getDisplayValue instead of raw sheet.cells.

### Typing on a merge-covered cell sets editingCell but no editor ever renders — keyboard deadlock
- **Where:** `src/App.tsx:1400` · correctness
- **Failure:** Merge A1:B2, select A1, press ArrowRight (selection lands on covered B1), type '5': nothing appears and the entire keyboard goes dead. The only recovery is double-clicking a different cell (which silently starts a new edit) — a confused user perceives a frozen app.
- **Fix sketch:** In handleKeyCommand, resolve the merge anchor for the active address before setEditingCell (edit the anchor, as Excel does), and make arrow navigation skip covered cells to the anchor.

### Keyboard navigation, Go To, name box, and Find never scroll the selection into view
- **Where:** `src/App.tsx:1409` · keyboard-navigation
- **Failure:** Press ArrowDown 30 times from A1 on a default viewport: the active cell moves below the fold and the grid does not follow — the user navigates blind. 'Go To B5000' selects B5000 but the viewport stays at the top; Find Next reports a match the user cannot see.
- **Fix sketch:** After selection changes from keyboard/goto/find, compute the target row's offset from rowMeasurements and adjust scrollRef.scrollTop/scrollLeft (accounting for zoom and frozen panes) so the active cell is visible.

### No performance regression tests for the core engine: full HyperFormula rebuild from a dense matrix on every edit **[FIXED]**
- **Where:** `src/App.tsx:191` · test-coverage
- **Failure:** User pastes a 30k-row dataset then types values into cells: each Enter blocks the main thread ~500ms+ (measured 490ms rebuild at 30k rows, plus React re-render), making the app feel hung; no test fails because no test measures this path.
- **Fix sketch:** Add timed regression tests (like pivot.test.ts) for createFormulaEngine build/rebuild at 10k-40k rows and for setCellContent+rebuild cycles; longer term switch to HyperFormula's incremental mutation API (setCellContents) instead of rebuild-per-edit and build from sparse input.

### Formula-bar edit is silently discarded (or committed to the wrong cell) when the user clicks a grid cell
- **Where:** `src/components/FormulaBar.tsx:89` · data-loss
- **Failure:** Select A1, type '=SUM(B1:B9' in the formula bar, then click cell C3 (intending, as in Excel, to insert a reference or at least commit): selection changes, the draft resets, blur commits a no-op — the whole formula is silently lost with no status message.
- **Fix sketch:** Track the edit target address when the formula input gains focus (or first change) and commit to that captured address on blur, before selection-driven draft resets; skip the draft-reset effect while the formula input is focused; add an Escape path that restores the original content.

### Grid does several unmemoized O(rowCount) passes and ~rowCount object allocations on every render (every keystroke, scroll event, and resize mousemove)
- **Where:** `src/components/Grid.tsx:117` · performance
- **Failure:** At 100k rows, typing one character in the cell editor performs ~4 full 100k-element passes and allocates ~100k measurement objects (~10MB GC churn) before painting -> visible typing latency; scrolling fires this per scroll event -> stutter and multi-hundred-ms GC pauses; dragging a column resize handle replays it per mousemove.
- **Fix sketch:** Memoize filteredRows on [sheet, formulaEngine] (not per render); use prefix-sum arrays + binary search for first/last visible row; derive row offsets arithmetically when rowHeights is empty (the common case); avoid the includes(0) scan by checking the first element.

### getVisibleRows re-evaluates every filtered row via the formula engine on every Grid render (no memo) - UI hang on large filtered ranges
- **Where:** `src/components/Grid.tsx:114` · performance
- **Failure:** 100k-row imported dataset with one active auto-filter: every scroll frame and every keystroke re-runs 100k getDisplayValue calls plus string matching - hundreds of ms to seconds per frame; the grid effectively hangs, defeating the row-virtualization that exists just below (lines 117-136).
- **Fix sketch:** useMemo filteredRows keyed on [sheet.filters, sheet.hiddenRows, sheet.rowCount, formulaEngine]; hoist normalizeRange out of the per-row check; only evaluate columns that actually have filters.

### Auto-filter dropdown choices scan the whole filter range for every header cell on every render, even with the menu closed
- **Where:** `src/components/Grid.tsx:607` · performance
- **Failure:** Auto-filter over 100k rows x 6 columns with the header row visible: each render performs 600k engine lookups plus Map/sort work - typing, selecting, or scrolling with the header on screen freezes the tab for seconds.
- **Fix sketch:** Compute choices lazily when the menu opens (inside the dropdown component or an onOpen handler) and memoize per (column, filters, formulaEngine).

### Row virtualization ignores CSS zoom — non-100% zoom renders the wrong rows / blank viewport when scrolled
- **Where:** `src/components/Grid.tsx:118` · rendering-desync
- **Failure:** 10k-row sheet at 200% zoom: scroll to outer offset 100000px. Actual top row is ~1785 but the code renders rows from ~3571, so the viewport lands inside the top spacer — the grid shows entirely blank cells. At 50% zoom the bottom half of the viewport is blank even before scrolling because lastVisibleIndex under-covers by 2x.
- **Fix sketch:** Divide scrollTop and clientHeight by (zoomLevel/100) before the row-window math (and multiply spacer heights accordingly), or apply zoom by scaling row heights/column widths numerically instead of CSS zoom.

### Paste while a cell editor is open is hijacked by the grid: clipboard writes to the sheet and the stale draft then overwrites it
- **Where:** `src/components/Grid.tsx:227` · data-loss
- **Failure:** Double-click B2, clear it, press Ctrl+V to paste a URL: the input stays empty, the URL is committed straight into B2 behind the editor, then clicking away commits the editor's empty draft, deleting the pasted URL. Pasting multi-line text while editing splatters cells below/right of B2 with no visual warning.
- **Fix sketch:** In the grid onPaste handler, return early (no preventDefault) when the event target is inside .cell-editor-shell (or when editingCell is set), letting the native paste edit the draft.

### Editing state survives editor unmount: scrolling away deadlocks the keyboard and can commit the draft into another sheet
- **Where:** `src/components/Grid.tsx:123` · correctness
- **Failure:** Edit A1 on Sheet1, type '999', wheel-scroll 50 rows down (editor unmounts silently, keyboard now dead), click the Sheet2 tab: Sheet2's A1 shows an editor containing '999'; clicking any cell commits 999 into Sheet2!A1, corrupting Sheet2 and losing the Sheet1 edit.
- **Fix sketch:** Commit or cancel the edit when the editing row leaves the render window (or pin the editing row so it never unmounts); clear/commit editingCell on sheet switch and on selection change; record the sheetId in editingCell and commit against it.

### Focus falls to document.body after committing/canceling an edit — keyboard navigation dead until the user clicks the grid again
- **Where:** `src/components/Grid.tsx:764` · keyboard-navigation
- **Failure:** Click A1, type '1', press Enter, then press ArrowDown and type '2': nothing happens — focus is on body, arrows and typing are ignored until the user clicks a cell again. Rapid keyboard data entry (the core spreadsheet workflow) is broken after every single commit.
- **Fix sketch:** After commit/cancel, call scrollRef.current?.focus() (grid container has tabIndex=0); on Enter-commit move selection one row down and on Tab-commit one column right.

### Right-click always collapses a multi-cell selection to one cell — context-menu Copy/Cut/Clear on a range is impossible
- **Where:** `src/components/Grid.tsx:699` · correctness
- **Failure:** Drag-select A1:C10, right-click inside the selection, choose Copy, paste elsewhere: only the right-clicked cell was copied. Similarly 'Clear contents' clears one cell instead of the 30 selected — silently wrong results.
- **Fix sketch:** In onContextMenu (and openCellContextMenu), keep the existing selection when the clicked cell is inside normalizeRange(selection); collapse only when clicking outside it.

### AutoFilter dropdown choices are recomputed for every header cell on every render, even with the menu closed
- **Where:** `src/components/Grid.tsx:606` · performance
- **Failure:** AutoFilter over a 50k-row × 12-column table with the header visible: each render performs 600k HyperFormula display-value calls plus 12 sorts. Scrolling or typing anywhere freezes for seconds; grid becomes unusable exactly on the large tables AutoFilter is meant for.
- **Fix sketch:** Compute choices lazily only when isAutoFilterMenuOpen for that column (e.g., in the menu component or a useMemo keyed on openAutoFilterColumn + sheet), and cache per column until the workbook changes.

### Conditional-format rule range values recomputed after every edit; top/bottom rules re-sort the whole range per cell per render
- **Where:** `src/components/Grid.tsx:149` · performance
- **Failure:** Apply a 'Top 10' rule to A1:A100000, keep ~30 rows of that column visible, and type in any cell: the commit clears the cache, the next render evaluates 100k display values, and each of the ~30 visible ruled cells re-parses and re-sorts 100k values (30 × O(n log n)) — multi-second freeze per keystroke.
- **Fix sketch:** Cache derived aggregates (sorted numbers, min/max, counts) per rule, not just raw strings; invalidate only when cells inside the rule range change instead of on every engine identity change.

### Vertical merges inflate the entire CSS grid row track, shifting all rows below and desyncing virtualization math
- **Where:** `src/components/Grid.tsx:612` · rendering-desync
- **Failure:** Merge A5:A8 (vertical): row 5 becomes 4 rows tall for every column, rows 6-8 still render beneath it, and all content below appears ~84px lower than the scrollbar/selection math expects; clicking near the bottom selects the wrong rows' cells relative to the visuals. Scroll a tall merge's anchor just off-screen: the merged region turns blank.
- **Fix sketch:** Absolutely position merge anchors over their covered area (position:absolute within the relatively-positioned row area) or set explicit gridRow/gridColumn spans; sum only visible rows/columns; keep anchors rendered while any covered row is in the window.

### PivotPanel wipes user's configuration on every selection change while open, and Create pivots whatever is selected at click time
- **Where:** `src/components/PivotPanel.tsx:22` · ux
- **Failure:** User selects A1:C100, opens the pivot builder, sets Rows=Region/Values=Sales/AVERAGE, clicks a cell to double-check a value; all dropdowns snap back to defaults (SUM of the third column) and the source range changes; if they don't notice, they create a wrong pivot.
- **Fix sketch:** Snapshot the source range and headers when the panel opens (or when the user confirms the range) and keep panel state independent of live selection; only reset via an explicit 'change source' action.

### Bar and line charts plot Math.abs(value): negative values rendered identical to positive
- **Where:** `src/components/SheetCharts.tsx:95` · correctness
- **Failure:** User charts monthly P&L [-10, 10, -10, 10] as a line chart: it renders as a flat line at full height; a bar chart of losses looks identical to profits. The visualization asserts wrong data; Excel draws negatives below the axis.
- **Fix sketch:** Compute min/max over signed values, place a zero baseline within the plot, and draw bars/points relative to it.

### AutoFit reads every cell of the selection through HyperFormula and crashes via Math.max spread on large selections
- **Where:** `src/lib/autoFit.ts:46` · performance
- **Failure:** Select a whole column (header click, 100k rows) and hit AutoFit Columns: 100k HyperFormula reads + a 100k-argument spread -> multi-second hang, and above ~125k rows the spread throws RangeError, crashing the operation/app. Ctrl+A + AutoFit = 2.6M reads.
- **Fix sketch:** Clamp the scanned rows to the populated bounding box, replace spread-max with a running max, and sample (Excel measures only the widest few thousand values for huge columns).

### Auto-fit spreads one array element per row into Math.max - RangeError on large column selections
- **Where:** `src/lib/autoFit.ts:46` · correctness
- **Failure:** User clicks the column-A header (selection = A1:A200000 after a big import) and runs Auto-fit column width (App.tsx:837): the handler throws RangeError and the action silently fails (console error); at 80k rows it doesn't throw but freezes the UI while making 80k engine+format lookups.
- **Fix sketch:** Accumulate the max in the row loop instead of building an array + spread; optionally sample/limit rows considered (Excel scans only used cells - iterate sheet.cells keys for that column instead of every row index).

### Filter numeric parser treats blank cells as 0 - blanks pass number filters and '(Blanks)' matches zeros
- **Where:** `src/lib/filters.ts:77` · correctness
- **Failure:** (1) Auto-filter: user unticks everything except '(Blanks)' -> every row displaying 0 stays visible too; ticking only '0' also keeps blank rows. (2) FilterPanel 'lessThan 100' -> all blank rows inside the filter range remain visible (Excel excludes blanks from numeric comparisons). Filtered totals/exports are silently wrong.
- **Fix sketch:** Return null for empty/whitespace input before Number(), mirroring conditionalFormatting.ts:133-141; treat blank-vs-blank equality as a dedicated case for the '(Blanks)' choice.

### Date handling broken: HF default dateFormats (DD/MM/YYYY) never overridden, so the app's own ISO autofill dates and US-format entries are text, making date arithmetic error **[FIXED]**
- **Where:** `src/lib/formulaEngine.ts:52` · excel-fidelity
- **Failure:** User autofills a date column (app produces 2024-01-15, 2024-01-16, ...), then writes =B2-B1 expecting 1; gets #VALUE!. DATEDIF/EOMONTH/comparisons against these cells all fail; sorting treats them as text.
- **Fix sketch:** Configure dateFormats to include 'YYYY-MM-DD' plus the target locale's format (and matching timeFormats), and store autofill/date entries in a format the engine parses.

### Cell dates parse day-first (DD/MM/YYYY): en-US dates are misread or silently left as text **[FIXED]**
- **Where:** `src/lib/formulaEngine.ts:51` · excel-fidelity
- **Failure:** User types 3/5/2026 meaning March 5; every date function (=A1+30, MONTH(A1)) silently computes against May 3. User types 12/25/2026; =A1+1 returns #VALUE!, SUM/COUNT skip it, sorting treats it as text. Excel (en-US) parses both as month-first dates.
- **Fix sketch:** Configure dateFormats: ['MM/DD/YYYY','MM/DD/YY','DD MMM YYYY', ...] (and timeFormats) to match the app locale, or make it locale-driven.

### Typing a date displays the raw serial number; typed percents/currency display raw decimals (no number-format inference on entry) **[FIXED]**
- **Where:** `src/lib/formulaEngine.ts:128` · excel-fidelity
- **Failure:** User types 25/12/2026 into an unformatted cell -> the cell shows 46381 instead of the date. User types 50% -> cell shows 0.5. Excel-correct behavior: entry that parses as date/percent/currency both stores the number and auto-applies the matching number format, so the cell keeps displaying 12/25/2026, 50%, $5.00.
- **Fix sketch:** After commit, query getCellValueDetailedType/getCellValueFormat and auto-set the cell's numberFormat (date/percent/currency); render date-typed values through the date formatter by default.

### KNOWN BUG: formula autocomplete swallows Enter — '=1+2' + Enter becomes '=SUM(' (root cause: empty-prefix default suggestions) **[FIXED]**
- **Where:** `src/lib/formulaSuggestions.ts:58` · correctness
- **Failure:** Type '=1+2' in a cell, press Enter -> editor value is replaced with '=SUM(' and nothing commits. Worse in the formula bar: select a cell already containing '=1+2' (draft='=1+2' shows the popup on focus), press Enter intending to confirm -> draft becomes '=SUM('; the next blur or Enter commits '=SUM(' over the stored formula — corrupting existing data.
- **Fix sketch:** In getFormulaSuggestions return [] when prefix is empty and formulaBody.length > 0 (show the default list only for exactly '='); additionally only let Enter accept a suggestion after explicit popup navigation (arrow/hover), reserving plain Enter for commit; make insertFormulaSuggestion replace only the typed function-name prefix.

### Shallow persistence validation lets corrupt sheets through, then render crashes -> permanent white screen on startup
- **Where:** `src/lib/persistence.ts:34` · data-loss
- **Failure:** One malformed entry in the stored JSON (partial write, extension interference, older/newer schema) passes isWorkbookModel; the app then throws during the first render on every launch. With no error boundary the user sees a blank page forever and cannot reach any UI to reset - recovery requires devtools localStorage surgery, i.e., effective loss of the whole workbook.
- **Fix sketch:** Deep-validate/sanitize per-sheet collections in migrateWorkbook (drop malformed filters/rules/charts, validate address keys, clamp rowCount/columnCount), and wrap the root in an ErrorBoundary that offers 'reset stored workbook'.

### Untested: workbook autosave runs unguarded localStorage.setItem + full JSON.stringify on every edit (quota crash, per-keystroke stall) **[FIXED]**
- **Where:** `src/lib/persistence.ts:7` · test-coverage
- **Failure:** User imports a ~30MB-as-JSON dataset (well under HyperFormula's row limit if wide but short, e.g. 30k rows x 20 cols): first edit triggers the save effect -> localStorage.setItem throws QuotaExceededError -> unhandled exception; on reload the workbook silently reverts to the last successfully saved state — user work lost.
- **Fix sketch:** Wrap saveWorkbook in try/catch surfacing a status-bar warning; debounce saves; add a persistence test with a Storage stub whose setItem throws, and an App-level test asserting the app survives and warns.

### Values displayed in scientific notation are silently dropped from SUM/AVERAGE/MIN/MAX
- **Where:** `src/lib/pivot.ts:241` · correctness
- **Failure:** A source column contains 0.0000001 (entered directly or produced by a formula); SUM/AVERAGE/MIN/MAX silently exclude it; a group whose values are all sub-1e-6 shows an empty cell as if it had no data.
- **Fix sketch:** Aggregate from raw typed cell values (hf.getCellValue returns numbers) instead of display strings; or allow strict Number() parsing (which accepts exponent notation) before the letter-based currency-text heuristic.

### Excel pivot feature set largely missing: no drill-down, one value field, no filters, no date grouping, no Show-Values-As, 5 of 11 aggregators
- **Where:** `src/lib/pivot.ts:3` · excel-fidelity
- **Failure:** Any user expecting Excel-parity pivots (the stated product goal, explicitly including drill-down) cannot perform the corresponding operations at all; e.g. double-clicking a pivot total does nothing instead of extracting the contributing rows.
- **Fix sketch:** Extend PivotConfig to arrays of value fields with per-field aggregator/show-as, add filter model and date-grouping pass, store row-index provenance per output cell to power drill-down extraction.

### Data validation validates the raw formula string, blocking formula entry into validated cells and flagging valid formula results as invalid
- **Where:** `src/lib/validation.ts:30` · excel-fidelity
- **Failure:** Cell B2 has number validation 0-100. User types '=A1*2' where A1=10 -> commit rejected with 'Enter a number between 0 and 100'. Excel accepts the formula and validates its result (20). Any budget/scorecard sheet mixing validation with formulas is unusable.
- **Fix sketch:** When content starts with '=', validate the engine's computed display value (pass a resolver into validateCellCommit and into Grid's isInvalidValidation) instead of the raw string.

### Data validation rejects any formula entry because it validates the literal formula text
- **Where:** `src/lib/validation.ts:30` · excel-fidelity
- **Failure:** Cell has whole-number validation 0..100. User types =5+5 -> commit blocked with "Enter a valid number". Excel accepts the entry and validates the evaluated result (10). Likewise "50%" against a 0..1 decimal rule is wrongly rejected.
- **Fix sketch:** Evaluate the candidate content (e.g. via a scratch HF sheet or post-commit engine value) and validate the computed value, matching Excel; divide percent literals by 100.

### Undo history is unbounded and each entry pins a full copy of the cells record
- **Where:** `src/lib/workbook.ts:1777` · performance
- **Failure:** Type into 500 cells of a 100k-cell sheet during a session: 500 retained workbook snapshots, each holding a distinct ~100k-entry cells object (several MB each) -> multi-GB heap growth -> tab OOM/crash after sustained editing. Also [...past, present] itself is O(edits) per commit.
- **Fix sketch:** Cap history (e.g. 100 entries, drop oldest), or store inverse deltas (address -> old value) instead of whole workbook snapshots.

### Whole-column formatting materializes per-cell format entries and per-cell read-only checks for the entire selection
- **Where:** `src/lib/workbook.ts:439` · performance
- **Failure:** Click column header (selects 100k rows) then Bold: 100k address strings for the read-only check + 100k new format objects created, cellFormatsEqual per cell, and a formats record now holding 100k entries — every later edit that spreads formats (setCellBorders, paste, sort) pays O(100k) forever after. Ctrl+A then Bold on 100k x 26 = 2.6M format objects -> hang/OOM. In Excel this is instant column-level formatting.
- **Fix sketch:** Add column-, row-, and sheet-level default format layers (resolve cell format as cell > row > column > sheet) instead of exploding ranges into per-cell entries; make ensureEditableRange short-circuit arithmetically when protection maps are empty.

### Renaming or adding a sheet whose name differs only in case from an existing sheet crashes the app (HF sheet names are case-insensitive, app uniqueness check is case-sensitive)
- **Where:** `src/lib/workbook.ts:2853` · correctness
- **Failure:** Workbook has 'Sheet1'. User renames the second tab to 'sheet1'. commitWorkbook stores the model, the render-time buildFromSheets throws SheetNameAlreadyTakenError, and the app white-screens.
- **Fix sketch:** Compare names case-insensitively (e.g. toLocaleLowerCase) in uniqueSheetName, matching both Excel and HyperFormula semantics.

### Structural ops and sheet rename never rewrite formulas on other sheets; cross-sheet references silently go stale or shift wrongly
- **Where:** `src/lib/workbook.ts:1891` · correctness
- **Failure:** Sheet2!B1 = '=Sheet1!A10'. User deletes row 3 on Sheet1. Sheet1's data moves up one row but Sheet2!B1 still reads A10 — a silently wrong value. Renaming 'Data' to 'Data2024' breaks every formula in the workbook that referenced 'Data'.
- **Fix sketch:** On structural ops, transform formulas workbook-wide with sheet-qualification awareness (shift a reference only if it targets the modified sheet); on rename, rewrite sheet-qualified references — or delegate both to HyperFormula's CRUD ops which do this correctly.

### Deleting the first or last row/column of a referenced range breaks the formula with #REF! instead of shrinking the range
- **Where:** `src/lib/workbook.ts:2316` · excel-fidelity
- **Failure:** B10 = '=SUM(A1:A5)'. User deletes row 5 (or row 1). Formula becomes '=SUM(A1:#REF!)' ('=SUM(#REF!:A4)') and displays an error; every downstream consumer of B10 errors too.
- **Fix sketch:** Treat range references (start:end) as a unit during shifting: clamp endpoints into the surviving region and only emit #REF! when the whole range is deleted.

### sortRange orders rows by raw formula text, not computed values, and does not re-anchor relative references after moving rows
- **Where:** `src/lib/workbook.ts:1488` · excel-fidelity
- **Failure:** Column C contains '=A1*B1' ... '=A10*B10' with computed values 5,3,9,... Sorting C ascending orders rows alphabetically by the formula strings ('=A10*B10' before '=A2*B2'), not by 5/3/9, and each moved formula now multiplies a different row's inputs than the one it travels with.
- **Fix sketch:** Sort by evaluated display values (the engine is available to the caller — pass a getDisplayValue like copyRichRange does) and translate relative references by the row displacement when relocating formula cells.

### Naming a range 'RC'/'R1C2' (R1C1-notation collision) passes app validation but crashes the engine build **[FIXED]**
- **Where:** `src/lib/workbook.ts:1020` · correctness
- **Failure:** User selects A1:B5, types 'R1C2' (or 'RC') into the name box to define a named range. The model commits, buildFromSheets throws NamedExpressionNameIsInvalidError, and the app white-screens.
- **Fix sketch:** Mirror HF's full validation in isValidNamedRangeName: also reject /^[rR][0-9]*[cC][0-9]*$/ (and ideally call HyperFormula's static/instance name validation).

### Sort compares raw cell content, so formula columns sort by formula text instead of computed value
- **Where:** `src/lib/workbook.ts:1488` · correctness
- **Failure:** Sheet has Amount=Qty*Price as formulas in column D under an auto-filter. User clicks 'Sort A to Z' on D (App.tsx sortAutoFilterColumn -> sortRange with sortColumn=D). Rows are ordered by the text of '=B2*C2' vs '=B10*C10' (string compare), not by the amounts - output order is effectively arbitrary and differs from Excel.
- **Fix sketch:** Pass a getDisplayValue/getCellValue callback into sortRange and sort on evaluated values (numbers before text like Excel), keeping raw content only for relocation.

### Insert/delete rows/columns never updates formulas on other sheets that reference the modified sheet
- **Where:** `src/lib/workbook.ts:1891` · excel-fidelity
- **Failure:** Sheet2!A1 contains =Sheet1!B5. User inserts a row above row 5 on Sheet1. Excel rewrites the formula to =Sheet1!B6; this app leaves =Sheet1!B5, which now silently points at the wrong (shifted) data.
- **Fix sketch:** Apply reference shifting to every sheet's formulas, but only to references that resolve to the modified sheet (bare refs on that sheet + qualified refs naming it); or delegate structure operations to HyperFormula's addRows/removeRows which handle this.

### Cut/paste (move) does not re-point formulas that reference the moved cells
- **Where:** `src/lib/workbook.ts:1274` · excel-fidelity
- **Failure:** B1 contains =A1*2. User cuts A1 and pastes it to D5. In Excel B1 becomes =D5*2 and keeps working; here B1 still reads =A1*2 against the now-empty A1 and silently returns 0/blank-based results.
- **Fix sketch:** After a move, scan workbook formulas for references intersecting the source range and rewrite them to the destination offset (or use HyperFormula's moveCells).

### Sorting uses raw stored text as the key: formula cells sort by their formula string, dates sort as text
- **Where:** `src/lib/workbook.ts:1488` · excel-fidelity
- **Failure:** A1:A3 contain =C1 (10), =C2 (2), =C3 (33). Sort ascending: Excel orders 2, 10, 33 by value; this app compares "=C1"/"=C2"/"=C3" as text and leaves 10, 2, 33.
- **Fix sketch:** Use formulaEngine.getCellValue per key cell for sorting (numbers < text < booleans < errors, per Excel), keeping raw content only for relocation.

### Cross-sheet formula references have zero test coverage; renameSheet does not rewrite them, breaking formulas on rename
- **Where:** `src/lib/workbook.ts:1667` · test-coverage
- **Failure:** User builds a summary sheet with '=Data!B2' formulas, then renames 'Data' to 'Data 2024': every cross-sheet formula in the workbook turns into #REF!, silently breaking the summary; undo is the only recovery.
- **Fix sketch:** On rename, rewrite sheet-qualified references in all cell formula strings (and named ranges); add formulaEngine tests for '=Sheet2!A1' evaluation and workbook tests for rename/delete with dependent formulas.

### xlsx import converts Excel date cells to ISO strings that the engine treats as text **[FIXED]**
- **Where:** `src/lib/xlsx.ts:1186` · excel-fidelity
- **Failure:** Import a workbook with an invoice-date column and =A2+30 due-date formulas: the formulas evaluate to #VALUE!, SUM/COUNT of dates give 0, date sorting becomes text sorting. In Excel the same file computes normally because dates are serial numbers.
- **Fix sketch:** Convert Date to an Excel serial number (respecting date1904) and set the cell's date numberFormat, instead of an ISO string; alternatively add ISO formats plus 'YYYY-MM-DDThh:mm:ss' to the HF dateFormats config.

### xlsx export writes numeric/boolean entries as text cells, so Excel computes different results than the app
- **Where:** `src/lib/xlsx.ts:430` · excel-fidelity
- **Failure:** User types 5 and 10 in A1:A2, =SUM(A1:A2) shows 15 in the app. Export to xlsx, open in Excel: A1/A2 are text (green triangles, left-aligned) and SUM(A1:A2)=0. Excel-correct behavior: numeric entries are stored as numbers in the file.
- **Fix sketch:** At export time, run the same coercion HF applies (or query the engine's cell value/type per address) and write real numbers/booleans/dates plus appropriate numFmt instead of raw strings.

### Shared-formula cells import as a reference to the master cell instead of the translated formula **[FIXED]**
- **Where:** `src/lib/xlsx.ts:1192` · excel-fidelity
- **Failure:** Excel file has B2 ==A2*2 filled down as a shared formula to B3:B10. After import, B3:B10 all become =B2 and display B2's value; editing A5 no longer changes B5. Excel-correct behavior: B5 evaluates =A5*2.
- **Fix sketch:** Use ExcelJS cell.formula (which resolves shared formulas) when cell.formulaType indicates shared, or translate the master formula with translateFormulaReferences using the master->slave offset.

### Untested: date cells from real Excel files import as raw ISO text strings and their number formats are dropped **[FIXED]**
- **Where:** `src/lib/xlsx.ts:1186` · excel-fidelity
- **Failure:** Open any .xlsx with a date column from real Excel: every date cell renders as '2024-05-01T00:00:00.000Z', =A2+1 returns an error/concatenation instead of the next day, and sorting the column gives text order; re-exporting writes text instead of dates.
- **Fix sketch:** Convert Date values to a date string/serial the formula engine understands and map the cell's numFmt to a displayFormat; add an import test constructing a workbook with ws.getCell().value = new Date(...) plus numFmt.


## MEDIUM

### Single-cell selection expands pivot source to the whole sheet's populated bounding box, not the current region
- **Where:** `src/App.tsx:2351` · excel-fidelity
- **Failure:** Sheet has a table in A1:C50 and a lone remark in H1. User clicks B2 and creates a pivot: source becomes A1:H50, header row includes blank cells D1:G1, creation fails with a header error — or, if H1 aligns as a header, unrelated data silently inflates COUNT/Grand Total.
- **Fix sketch:** Implement current-region detection (flood-fill of contiguous non-empty cells from the active cell), matching Excel.

### Pivot labels starting with '=' are pasted as live formulas into the pivot sheet, corrupting output
- **Where:** `src/App.tsx:1111` · correctness
- **Failure:** Source 'Code' column contains display values '=A1', '=1+1' (built by concatenation formulas); the created pivot sheet renders them as the pivot sheet's own A1 content and '2' — row labels are corrupted and totals become misattributed to wrong-looking labels.
- **Fix sketch:** When writing pivot output, escape values that would parse as formulas (prefix apostrophe / store as explicit text), or add a raw-text paste mode to pasteMatrix.

### Pivot groups/labels use raw engine values, bypassing the number formats the grid shows (dates pivot as serial numbers)
- **Where:** `src/App.tsx:2343` · excel-fidelity
- **Failure:** User pivots orders by a date column: every row label in the created Pivot sheet is a 5-digit serial number instead of a date; column headers for a month field read '45992', '46023', ...
- **Fix sketch:** Pass each cell through formatDisplayValue with its resolved CellFormat when building pivot source rows (and carry the format onto the output label cells).

### Formula-audit dependents scan iterates all populated cells on every selection change
- **Where:** `src/App.tsx:2281` · performance
- **Failure:** Sheet with 100k populated cells of which 20k are formulas: every arrow-key selection move performs a 100k-entry scan + 20k regex extractions -> tens to hundreds of ms added to every navigation keystroke, compounding with the other per-selection scans.
- **Fix sketch:** Compute dependents only while the audit panel is open, and cache extracted references per formula string keyed by workbook version.

### commitWorkbook uses stale closure history with non-functional setState — concurrent commits in one React batch silently drop edits
- **Where:** `src/App.tsx:252` · correctness
- **Failure:** User edits a cell (commit fires on blur) and in the same tick clicks 'Bold' (second commit). The blur commit is computed and then clobbered by the bold commit built from the pre-edit workbook: the typed value disappears and is not in undo history.
- **Fix sketch:** Route all mutations through a single functional-updater path: setHistory(current => commitHistory(current, mutate(current.present))), or a reducer.

### Replace All interprets '$' replacement patterns instead of treating replacement literally
- **Where:** `src/App.tsx:2446` · correctness
- **Failure:** Find 'price' Replace All with 'price ($$)' -> cells get 'price ($)'; replace 'x' with '$&y' duplicates the matched text instead of inserting the literal '$&y'. Excel inserts the replacement literally.
- **Fix sketch:** Use a replacer function: value.replace(regex, () => replacement).

### Replace on a numeric cell with a non-matching query falsely reports 'Replaced', converts number to string, and skips find-next
- **Where:** `src/App.tsx:1658` · correctness
- **Failure:** Active cell holds number 123 (imported); Find 'abc', click Replace -> status 'Replaced A1', an undo step is consumed, the cell silently becomes the string '123', and the cursor never moves to the real match elsewhere.
- **Fix sketch:** Compare String(currentContent) with nextContent; after a successful replace call handleFindNext().

### Deleting the last visible sheet activates a hidden sheet, leaving an empty tab bar and violating the visible-sheet invariant
- **Where:** `src/App.tsx:1445` · correctness
- **Failure:** Two sheets; hide Sheet2; delete Sheet1 -> active sheet is hidden Sheet2, the tab strip shows only '+', 'Unhide sheets' state is inconsistent, and hide/protection logic operates on a sheet the UI claims is hidden.
- **Fix sketch:** Guard in handleDeleteSheet (or deleteSheet): refuse to delete the last visible sheet, or auto-unhide the fallback sheet; set a status message when deletion is refused.

### Undo/redo never clamps or resets selection/editing state restored across structural changes
- **Where:** `src/App.tsx:1352` · correctness
- **Failure:** Edit on Sheet1, switch to Sheet2 (tab switch is not committed to history, 2084-2091), press Ctrl+Z: the view jumps back to Sheet1 with Sheet2's selection coordinates still active; pressing Delete clears Sheet1 cells at those coordinates without the user ever selecting them there.
- **Fix sketch:** After undo/redo, clamp selection to the restored active sheet's bounds and clear editingCell/cellContextMenu; optionally track per-sheet selections.

### Name box and Go To treat any letters+digits token as a cell reference, shadowing named ranges
- **Where:** `src/App.tsx:344` · excel-fidelity
- **Failure:** User defines 'Sales2024' via the Named Ranges panel, later types 'Sales2024' into Go To -> selection jumps to the clamped max cell of the active sheet instead of the named range.
- **Fix sketch:** Check named ranges before reference parsing (or reject refs whose column/row exceed the sheet's addressable space), and validate name collisions in isValidNamedRangeName.

### Two open tabs silently clobber each other's autosaved workbook **[FIXED]**
- **Where:** `src/App.tsx:216` · data-loss
- **Failure:** User opens the app in a second tab (loads snapshot T0), does an hour of work in tab 1 (persisted), then makes one stray edit in tab 2: tab 2 writes T0+1 edit, erasing the hour of work from storage with no warning.
- **Fix sketch:** Listen for the 'storage' event to detect external writes and warn/merge, or stamp saves with a session id + monotonically increasing revision and refuse stale overwrites.

### CSV export writes raw formula text and raw entries instead of evaluated display values
- **Where:** `src/App.tsx:2165` · excel-fidelity
- **Failure:** Sheet uses HF-flavored constructs or values only the app computed; exported CSV opened in another tool (pandas, DB import) contains literal "=SUM(A1:A2)" strings instead of numbers.
- **Fix sketch:** Use formulaEngine.getDisplayValue (or getVisibleCellText) per cell when building CSV rows.

### Arrow navigation lands on hidden/filtered rows and columns — the active cell becomes invisible and edits target unseen cells
- **Where:** `src/App.tsx:1421` · excel-parity
- **Failure:** Hide row 2, select A1, press ArrowDown: selection is now on hidden A2 (no visible highlight). Typing '9' starts editing invisible A2; the value silently lands in a hidden row. Excel skips to A3.
- **Fix sketch:** In moveSelection, advance to the next index not present in hiddenRows/hiddenColumns and not filtered out (reuse getVisibleRows results), and snap to merge anchors.

### Copy puts raw formula text on the system clipboard instead of display values
- **Where:** `src/App.tsx:1252` · excel-parity
- **Failure:** Copy a results column of formulas and paste into an email, Notepad, or Google Sheets: recipients get '=SUM(A1:A2)' strings (which in another spreadsheet re-evaluate against unrelated data) instead of the numbers displayed on screen.
- **Fix sketch:** Use display values (getVisibleCellText, already passed to copyRichRange) for the text/plain payload; keep raw formulas only in the internal rich clipboard; optionally add a text/html payload for formatted external paste.

### Pasting a single copied cell into a multi-cell selection fills only one cell
- **Where:** `src/App.tsx:1218` · excel-parity
- **Failure:** Copy A1 containing '=B1*2', select C1:C100, press Ctrl+V: only C1 receives the formula; the other 99 cells stay empty. In Excel all 100 cells are filled with translated formulas.
- **Fix sketch:** When the selection is larger than the clipboard matrix and dimensions are compatible, tile the matrix (with per-tile formula translation) across the normalized selection.

### Shell key handler hijacks Enter/Space/typing when focus is on toolbar buttons — toolbar is not keyboard-operable
- **Where:** `src/App.tsx:1279` · keyboard-navigation
- **Failure:** Tab to the Bold toolbar button and press Enter or Space: instead of toggling bold, the active cell enters edit mode (Enter was preventDefault'ed so the button never activates). Keyboard-only users cannot operate any toolbar control.
- **Fix sketch:** Also bail out when the event target is a button/element with role (e.g., target.closest('button, [role]')) or restrict handleShellKeyCommand to events originating from the grid region.

### Import failure paths untested: corrupt/invalid XLSX or CSV error handling has no coverage
- **Where:** `src/App.tsx:1552` · test-coverage
- **Failure:** User picks a password-protected or truncated .xlsx: importWorkbookFromXlsx rejects with a low-level zip error; the only feedback is a cryptic status-bar string (or the app state silently unchanged); a regression that turns this rejection into an unhandled throw (e.g. error before the promise chain) would never be caught by tests.
- **Fix sketch:** Add App tests firing the change event with a garbage File and asserting a friendly status message and intact workbook; map known ExcelJS errors to user-readable text.

### Context-menu Paste ignores the system clipboard: disabled without an internal copy and can paste stale internal content
- **Where:** `src/components/CellContextMenu.tsx:113` · clipboard
- **Failure:** Copy a table in Excel, right-click a cell here, choose Paste: the item is greyed out (or pastes an old internal copy) even though Ctrl+V would work. User assumes paste is broken.
- **Fix sketch:** In context-menu Paste, try navigator.clipboard.readText() and route through pasteText (falling back to richClipboard); tag the internal clipboard with an ownership marker rather than relying on exact text equality.

### Escape cannot cancel a formula-bar edit; abandoned draft is committed on blur
- **Where:** `src/components/FormulaBar.tsx:112` · excel-fidelity
- **Failure:** Click a cell containing a good formula, start retyping in the formula bar, change your mind, press Escape (nothing happens), click another cell -> the half-typed junk replaces the original formula.
- **Fix sketch:** On Escape with no suggestions: reset the draft to the committed cell content and blur without committing (mirror onNameBoxCancel).

### Merge lookup scans the merges array for every rendered cell
- **Where:** `src/components/Grid.tsx:1118` · performance
- **Failure:** Import an xlsx with 5,000 merged ranges (common in report-style workbooks): ~1,000 visible cells x 5,000 merges = 5M range checks + allocations per render, on every keystroke/scroll/selection change -> hundreds of ms per frame, grid feels frozen while scrolling.
- **Fix sketch:** Build a Map from covered address (or row-bucketed interval index) to merge info once per sheet version and do O(1) lookups per cell.

### No column virtualization: every non-hidden column is rendered for each visible row
- **Where:** `src/components/Grid.tsx:550` · performance
- **Failure:** Import an xlsx whose used range extends to column ALZ (1,000 columns; Excel allows 16,384): ~48 visible rows x 1,000 columns = 48,000 cell divs (plus spans/handles) in the DOM, each re-running its computation block per render -> initial paint of several seconds and every re-render (keystroke, scroll) far beyond frame budget.
- **Fix sketch:** Virtualize columns with the same spacer technique used for rows (prefix-sum widths + binary search on scrollLeft), and hoist per-cell work that doesn't depend on the cell (autoFilterRange normalization, border style parsing) out of the loop.

### No Shift+Click, Shift+Arrow, or Ctrl+A selection extension — keyboard range selection is impossible
- **Where:** `src/components/Grid.tsx:686` · excel-parity
- **Failure:** Click A1, Shift+Click D10 (or hold Shift and press arrows): instead of extending the selection A1:D10 as in Excel, the selection collapses to the clicked/next cell. Users cannot select ranges without a mouse drag.
- **Fix sketch:** On mousedown with shiftKey, keep selection.start and set end to the clicked cell; on Shift+Arrow, move selection.end only; map Ctrl+A to the whole used range/sheet.

### Formula autocomplete hijacks Enter and Left/Right arrows in the cell editor — some formulas cannot be committed with Enter
- **Where:** `src/components/Grid.tsx:791` · editing
- **Failure:** Type '=TRUE' and press Enter: instead of committing, the input becomes '=TRUE(' and stays in edit mode. Type '=SU', notice a typo, press ArrowLeft to fix it: the caret doesn't move — the suggestion index cycles instead. Same behavior in the formula bar (FormulaBar.tsx:94-110).
- **Fix sketch:** Reserve Up/Down for suggestion navigation and Tab for accept; let Enter commit the typed text (accept suggestion only via Tab/click); never intercept Left/Right.

### Drag-select and drag-fill cancel on mouseleave, with no auto-scroll or fill preview — ranges beyond the viewport are unreachable by drag
- **Where:** `src/components/Grid.tsx:231` · drag-fill
- **Failure:** Select A1 and drag the fill handle downward to the bottom edge of the grid intending to fill 200 rows: the view doesn't scroll, and the moment the pointer crosses the container edge the whole autofill is cancelled with no feedback — nothing fills on release.
- **Fix sketch:** Track drag with window-level mousemove/mouseup instead of relying on cell mouseenter/container mouseleave; auto-scroll when the pointer nears an edge; render a dashed border over autoFillDrag.target cells.

### AutoFill only supports down/right; up/left or diagonal drags silently do nothing
- **Where:** `src/components/Grid.tsx:1035` · drag-fill
- **Failure:** Put 10 in A10 and drag the fill handle up to A1: nothing happens (target stays equal to source, so onAutoFill isn't even called, or status shows 'Drag AutoFill down or right'). Dragging diagonally down-right also silently no-ops instead of picking the dominant axis.
- **Fix sketch:** Extend createAutoFillTarget/autoFillRange for up/left with negated series steps; choose the dominant axis for diagonal drags; treat shrinking drags as clearing the excluded cells.

### No column virtualization and O(merges) scan per cell — wide sheets multiply render cost
- **Where:** `src/components/Grid.tsx:110` · performance
- **Failure:** Import a sheet with 300 used columns: ~40 visible rows × 300 columns = 12k cells per render, each scanning the merges array — scrolling and typing become visibly laggy even though row virtualization is in place.
- **Fix sketch:** Virtualize columns using scrollLeft (mirroring the row logic), and index merges by row/column for O(1) lookup.

### Embedded charts re-read their entire source range through HyperFormula on every App render
- **Where:** `src/components/SheetCharts.tsx:32` · performance
- **Failure:** Create a chart from a 50k-row selection: every keystroke in any cell editor re-executes 50k+ getCellValue calls per chart before paint -> seconds of added latency per keystroke while the chart exists.
- **Fix sketch:** Memoize chartData per chart on [chart.range, formulaEngine] (workbook version), and cap chart source rows (the SVG renders at most a handful of labels anyway).

### Color pickers commit a history entry and full engine rebuild on every onInput tick while dragging
- **Where:** `src/components/Toolbar.tsx:740` · performance
- **Failure:** Drag across the fill-color picker for two seconds on a large sheet: dozens of commits freeze the UI, and afterwards Ctrl+Z must step through every intermediate shade before reaching the real previous action.
- **Fix sketch:** Use onChange (fires on picker close) or debounce/coalesce consecutive color commits into a single history entry.

### AutoSum on a rectangular block inserts one SUM of the whole 2D range instead of per-column sums
- **Where:** `src/lib/autoSum.ts:43` · excel-fidelity
- **Failure:** User selects A1:C5 of numbers and presses AutoSum expecting column totals in A6:C6; instead A6 gets =SUM(A1:C5) (the grand total in the first column) and B6/C6 stay empty - totals row is wrong and misplaced.
- **Fix sketch:** For block selections, return a plan per column ({row: end.row+1, column: c} with =SUM(colRange)); keep current behavior only for vector selections.

### Charts use only the first two columns of the range; single-column numeric selections produce no chart
- **Where:** `src/lib/charts.ts:21` · excel-fidelity
- **Failure:** (1) User selects B2:B13 (twelve monthly totals) and creates a chart: creation is refused, though Excel charts it with index categories. (2) User selects A1:D13 expecting three series: only column B is plotted, C/D silently discarded. (3) A row with a missing label disappears from the chart entirely.
- **Fix sketch:** Support value-only ranges (synthesize labels '1..n'), plot additional columns as extra series (or at minimum warn), and keep blank-label rows with an empty category label.

### Percent and comma stripping without semantics: '50%' compares as 50, '1,0' passes number validation as 10
- **Where:** `src/lib/conditionalFormatting.ts:139` · excel-fidelity
- **Failure:** CF rule 'less than 1' on a column of percent-displayed values never fires ('50%' -> 50). Validation min=5,max=15 accepts '1,0'; the cell then breaks downstream SUM/AVERAGE because it is stored and evaluated as text - a validation bypass producing silently wrong totals.
- **Fix sketch:** Parse percent as value/100; only strip commas in valid thousands positions (or require strict numeric); after validation, ensure the accepted value is numeric to the engine too.

### "Number" display format uses grouping with 0-2 decimals and disagrees with both Excel's Number format and the app's own xlsx export format
- **Where:** `src/lib/displayFormat.ts:3` · excel-fidelity
- **Failure:** Apply Number format to 1234.567: app shows "1,234.57"; the exported file opened in Excel shows "1,234.567" — same workbook, two renderings; neither matches Excel's Number preset "1234.57".
- **Fix sketch:** Fix decimals (min=max=2) and decide on grouping to mirror Excel presets, and make XLSX_NUMBER_FORMATS carry the identical format codes.

### Date display: non-ISO date text is parsed in local time but formatted in UTC (off-by-one day), and the hardcoded style differs from Excel's short date
- **Where:** `src/lib/displayFormat.ts:97` · excel-fidelity
- **Failure:** User in UTC+5:30 types "March 5, 2026" into a date-formatted cell: local midnight = Mar 4 18:30 UTC, so the cell displays "Mar 4, 2026". Excel shows March 5.
- **Fix sketch:** Parse text dates as timezone-less civil dates (regex/manual parse to UTC), and offer Excel-style m/d/yyyy as the default date rendering.

### No automatic percent entry: typing 5 into a percent-formatted cell shows 500%
- **Where:** `src/lib/displayFormat.ts:46` · excel-fidelity
- **Failure:** Cell formatted as percent shows 50%. User retypes 60 (without %). App displays 6000% (value 60); Excel displays 60% (value 0.6).
- **Fix sketch:** On commit into a percent-formatted cell, divide bare numeric entries >= 1 by 100 (matching Excel's rule) or at minimum divide all bare numerics.

### 1900 date system off by one vs Excel for January/February 1900 (leapYear1900 not set) **[FIXED]**
- **Where:** `src/lib/formulaEngine.ts:51` · excel-fidelity
- **Failure:** Workbook imported from Excel contains 1/15/1900 (serial 15). Rendering/re-export shifts it to 1/14/1900; DATE(1900,2,29) silently becomes March 1. Excel-correct: keep Lotus-compatible serials with the phantom 2/29/1900.
- **Fix sketch:** Set leapYear1900: true with nullDate {year:1899, month:12, day:31} per HF docs for Excel compatibility, and align displayFormat's epoch.

### Formula referencing an empty cell displays blank instead of 0 (evaluateNullToZero not set) **[FIXED]**
- **Where:** `src/lib/formulaEngine.ts:51` · excel-fidelity
- **Failure:** User enters =A1 in B1 with A1 empty. Cell shows nothing; Excel shows 0. Downstream UI (selection summary Count) also counts it as empty.
- **Fix sketch:** Pass evaluateNullToZero: true in the HF config.

### String comparisons are accent-insensitive ("a"="á" is TRUE); Excel is accent-sensitive **[FIXED]**
- **Where:** `src/lib/formulaEngine.ts:51` · excel-fidelity
- **Failure:** =COUNTIF(A:A,"Ana") also counts "Aná"; =IF(A1="resume",...) fires for "resumé". Excel would treat them as different strings.
- **Fix sketch:** Set accentSensitive: true (leave caseSensitive false, which matches Excel).

### Boolean results render as lowercase "true"/"false" instead of TRUE/FALSE **[FIXED]**
- **Where:** `src/lib/formulaEngine.ts:128` · excel-fidelity
- **Failure:** =1>0 displays "true"; typing TRUE into a cell displays "true". Excel shows TRUE.
- **Fix sketch:** In formatCellValue, special-case booleans to "TRUE"/"FALSE".

### Mixed-fraction entry "1 1/2" becomes a date serial instead of 1.5 **[FIXED]**
- **Where:** `src/lib/formulaEngine.ts:51` · excel-fidelity
- **Failure:** User types 1 1/2 expecting one-and-a-half; the cell displays 37257 and SUMs are inflated by a date serial. Excel stores 1.5.
- **Fix sketch:** Fixing dateFormats to month-first removes this particular collision; full parity needs a fraction-entry parser before handing content to HF.

### Fill and copy/paste formula translation corrupts single-quoted sheet names ending in digits
- **Where:** `src/lib/formulaReferences.ts:25` · correctness
- **Failure:** User has a sheet named 'Q4 Data', writes ='Q4 Data'!B2 on another sheet, and drags the fill handle down. Every filled cell references a nonexistent sheet ('Q5 Data', 'Q6 Data', ...) and evaluates to an error.
- **Fix sketch:** Extend the segment scanner to skip single-quoted spans ('' escapes) in addition to double-quoted strings, and also guard tokens immediately followed by a closing quote.

### Over-lenient numeric coercion turns malformed text into wrong numbers ('1,23' -> 123, '4 5' -> 45)
- **Where:** `src/lib/pivot.ts:240` · correctness
- **Failure:** A CSV import leaves European-formatted amounts as text ('1.234,56'); pivot SUM produces plausible-looking but wrong totals (1.23456 each) with no error, or '12,3' inflates 12.3 to 123.
- **Fix sketch:** Only strip commas in valid thousand-separator positions (regex /^\d{1,3}(,\d{3})*(\.\d+)?$/), strip at most one leading currency symbol, and reject internal spaces.

### Error values in source data are silently treated as text instead of surfacing in the pivot
- **Where:** `src/lib/pivot.ts:184` · excel-fidelity
- **Failure:** One revenue formula in the source hits #DIV/0!; pivot SUM quietly under-reports the group's total while COUNT includes the errored row, and the user has no signal anything was excluded.
- **Fix sketch:** Detect error strings (/^#(DIV\/0!|N\/A|NAME\?|NULL!|NUM!|REF!|VALUE!|ERROR!)/) in addAggregateValue and mark the aggregate as errored, rendering the error token in the output cell.

### Multi-level row pivots have no subtotal rows and repeat outer labels (flat cross-join only)
- **Where:** `src/lib/pivot.ts:85` · excel-fidelity
- **Failure:** User pivots Region+Product to compare regions: the output contains only leaf combinations and one grand total; per-Region totals — the primary reason to add a second level — are absent.
- **Fix sketch:** After sorting groups, emit an '<outer> Total' row whenever the outer key changes, aggregating from per-outer AggregateStates.

### Blank dimension values produce empty header cells and unlabeled rows instead of '(blank)'
- **Where:** `src/lib/pivot.ts:84` · excel-fidelity
- **Failure:** Source has 3 rows with an empty Region; the pivot sheet gains a nameless row with real numbers just above Grand Total, and with a column field a nameless column appears before Grand Total; users can't tell what it represents.
- **Fix sketch:** Map "" to the literal label "(blank)" for row keys and column values at output time (keep "" for internal grouping/sorting).

### Column widths and row heights are hard-clamped to 360px/96px, distorting resizes and imported sheets
- **Where:** `src/lib/sheetDimensions.ts:4` · excel-fidelity
- **Failure:** Import an xlsx with a 600px 'Description' column and 150px wrapped rows: content renders truncated in a 360px column and 96px rows, misrepresenting the file; dragging the resize handle further has no effect and the width silently commits at 360.
- **Fix sketch:** Raise the caps to Excel's limits (or remove the max entirely) and only clamp to sane minimums; clamp imported values with the same policy used by the exporter.

### Deleting rows/columns silently discards merges, conditional formats, filters, charts, and named ranges whose range endpoint falls in the deleted span
- **Where:** `src/lib/workbook.ts:2037` · data-loss
- **Failure:** A conditional-format rule covers A1:A100; user deletes row 1 (a header they no longer want). The entire rule vanishes for the remaining 99 rows. A chart anchored at D2 disappears when row 2 is deleted.
- **Fix sketch:** When an endpoint is in the deleted span, clamp it to the nearest surviving row/column and drop the object only if the whole range is deleted; relocate chart anchors instead of deleting charts.

### Clear-all/clear-formats delete entire conditional-format rules that merely intersect the cleared range
- **Where:** `src/lib/workbook.ts:347` · data-loss
- **Failure:** Rule highlights negatives across B2:B5000. User selects B10 and hits Clear formats. All 4,999 other cells lose their conditional highlighting in one keystroke, with no error and only whole-workbook undo to recover.
- **Fix sketch:** Subtract the cleared range from each rule's range (splitting into up to 4 remainder rectangles) instead of dropping intersecting rules.

### Undo history is unbounded and structural ops rebuild every per-sheet map, so memory grows without limit on large workbooks
- **Where:** `src/lib/workbook.ts:1777` · performance
- **Failure:** User performs 300 edits/inserts on a 30,000-row sheet in one session; heap grows by hundreds of MB of retained snapshots and the tab eventually becomes unresponsive or crashes.
- **Fix sketch:** Cap history depth (e.g. 100 entries) in commitHistory, or store inverse patches instead of whole-workbook snapshots.

### Whole-row/column/select-all operations materialize a per-cell entry for every address in the range
- **Where:** `src/lib/workbook.ts:439` · performance
- **Failure:** User clicks the column A header on a 40k-row sheet and presses Ctrl+B: multi-second freeze, followed by permanently slower snapshots/saves due to 40k retained format entries.
- **Fix sketch:** Introduce row/column/sheet-level default formats (like Excel's column styles) and only materialize per-cell entries for true per-cell overrides; iterate ranges lazily.

### Undo history is unbounded; every commit retains a full workbook snapshot chain
- **Where:** `src/lib/workbook.ts:1777` · performance
- **Failure:** Repeatedly paste/sort a 100k-cell region for an hour -> hundreds of full cell-map copies pinned by the past array -> tab memory balloons until crash.
- **Fix sketch:** Cap past length (e.g. 100 entries, like Excel) in commitHistory.

### Deleting a row/column containing a range endpoint silently deletes whole charts, CF rules, filters, and named ranges
- **Where:** `src/lib/workbook.ts:1971` · data-loss
- **Failure:** Chart built on A1:B10; user deletes row 1 (the header row). The chart vanishes entirely. Similarly, a conditional format on A1:A100 disappears for all 99 surviving rows after deleting just row 1, and a named range dies when its first row is removed.
- **Fix sketch:** When an endpoint is inside the deleted band, clamp it to the band edge instead of returning null; only drop the object when the whole range is within the deleted band.

### Clearing conditional formats removes every rule that intersects the selection, not just the selected cells
- **Where:** `src/lib/workbook.ts:849` · excel-fidelity
- **Failure:** Rule covers A1:A1000. User selects A500 and clicks 'Clear conditional formats' in the panel (App.tsx:526-535 passes the selection): the entire rule disappears for all 1000 cells with only a generic status message.
- **Fix sketch:** Subtract the cleared range from each rule's range (splitting into up to 4 remainder rectangles) and only delete rules whose range is fully covered.

### Validation rules are stored per cell address - full-column apply creates hundreds of thousands of entries
- **Where:** `src/lib/workbook.ts:802` · performance
- **Failure:** User selects column A (200k rows after an import) and applies a list validation: the handler materializes 200k addresses + 200k rule clones (multi-second stall), the workbook JSON grows by tens of MB (guaranteeing the localStorage quota crash), and the validation panel summary recompute becomes visibly slow.
- **Fix sketch:** Store validations as range-scoped rules (like conditionalFormats) with per-cell lookup by containment; keep the summary as a trivial pass-through.

### Autofill only extends dates written as ISO text, and the filled values are text to the formula engine
- **Where:** `src/lib/workbook.ts:2254` · excel-fidelity
- **Failure:** User types 3/5/2026 and drags the fill handle down expecting 3/6, 3/7...; gets identical copies. With 2026-03-05 they get an incrementing column, but =B1-A1 on those cells returns #VALUE!. Excel increments any recognized date and keeps values as true dates.
- **Fix sketch:** Detect date-typed cells via the engine (getCellValueDetailedType), step the serial number, and write back a value the engine parses as a date (or a serial plus date format).

### Undo history is an unbounded array of workbook snapshots with no size-bound tests
- **Where:** `src/lib/workbook.ts:1777` · test-coverage
- **Failure:** User imports a 30k-row CSV (≈1MB+ model), then performs 50 sorts/filters/imports over a session: each whole-sheet-replacing operation pins another multi-MB snapshot; browser tab memory grows unbounded until the tab is killed — no test would catch the regression or enforce a cap.
- **Fix sketch:** Cap history length (e.g. slice past to the last 100 entries in commitHistory) and add a unit test asserting the cap; consider command-based (diff) history for sheet-replacing operations.

### XLSX tests only round-trip self-generated files — no real-Excel fixtures, so import fidelity is untested by construction
- **Where:** `src/lib/xlsx.test.ts:44` · test-coverage
- **Failure:** Any importer regression or gap that real Excel files trigger (e.g. theme colors resolving to undefined, cached results ignored for unsupported functions) ships with a fully green suite, because no test input ever contains those constructs.
- **Fix sketch:** Commit a small fixtures/ directory with real-Excel .xlsx files (dates, shared formulas, styled cells, tables) and assert imported model contents; alternatively build them in-test with raw ExcelJS APIs the exporter doesn't use.

### Excel number-format codes are silently dropped on import (only 5 exact preset strings survive)
- **Where:** `src/lib/xlsx.ts:1145` · excel-fidelity
- **Failure:** Import a workbook where a column is formatted 0.00% and another m/d/yyyy: percentages display as raw decimals (0.153) and dates as serials/ISO text. Excel renders 15.30% and 3/5/2026.
- **Fix sketch:** Classify imported numFmt codes by pattern (contains %, date tokens dmyh, currency symbols) into the closest preset, and long-term support real format-code rendering.


## LOW

### All cells left-aligned by default; Excel right-aligns numbers/dates and centers booleans/errors
- **Where:** `src/App.css:1825` · ux
- **Failure:** A column mixing the number 100 and the text '100 ' looks identical; in Excel the misaligned entry is instantly visible. Users cannot spot text-number import problems (which this app's xlsx export also creates).
- **Fix sketch:** When no explicit alignment is set, choose alignment from the engine value type (getCellValueDetailedType).

### Panel dedupes duplicate headers that the engine then rejects, so Create always fails on such sources
- **Where:** `src/App.tsx:2469` · ux
- **Failure:** Source table has two 'Sales' columns (common after joins/exports); user configures the pivot normally, presses Create, and gets a status-bar rejection with no way to proceed short of renaming source columns.
- **Fix sketch:** Auto-disambiguate duplicates when building source rows (Sales, Sales_2) as Excel does, or surface the validation error inside the panel before Create.

### Ctrl+F leaves the Function Library and Named Ranges panels open (inconsistent panel-exclusion sets)
- **Where:** `src/App.tsx:1339` · ux
- **Failure:** Open Formulas -> Function library, press Ctrl+F: the Find panel opens stacked on/under the Function Library panel; other panel invocations would have closed it.
- **Fix sketch:** Replace all hand-rolled lists with closeFloatingPanels() + open the requested panel.

### Insert -> Pivot always opens the panel instead of toggling like every other panel button
- **Where:** `src/App.tsx:1847` · ux
- **Failure:** User clicks Insert -> Pivot table to close the panel (as works for Chart) -> panel stays open; only the X button closes it.
- **Fix sketch:** setPivotPanelOpen((isOpen) => !isOpen) like the others.

### Function Library insert only sets the formula-bar draft; the inserted function is silently discarded on the next selection change
- **Where:** `src/App.tsx:664` · ux
- **Failure:** Formulas -> Function library -> insert XLOOKUP -> click cell B2 to select the lookup value -> the formula bar now shows B2's content; '=XLOOKUP(' is gone.
- **Fix sketch:** Enter editing mode (setEditingCell) or focus the formula input after inserting; ideally support ref-picking while a formula draft is active.

### Format painter is neither applied nor canceled by keyboard selection moves
- **Where:** `src/App.tsx:1425` · ux
- **Failure:** Arm format painter, arrow to the intended destination cell -> nothing painted; days later a stray click on an unrelated cell suddenly repaints it.
- **Fix sketch:** Route moveSelection through handleSelectionChange, or cancel the painter on keyboard navigation.

### Formula audit dependents miss cross-sheet, named-range, and whole-column references
- **Where:** `src/App.tsx:2281` · correctness
- **Failure:** Sheet2!B1 contains '=Sheet1!A1*2'; user audits Sheet1!A1 -> 'No dependents' is shown, and a formula '=SUM(Sales)' (named range covering the cell) is likewise not listed - misleading impact analysis before an edit.
- **Fix sketch:** Scan all sheets' cells, resolve sheet-qualified refs and named ranges to concrete ranges before the intersection test, and add column/row-span reference support.

### Syntactically invalid formulas are committed and display #ERROR!; Excel refuses the entry
- **Where:** `src/App.tsx:288` · excel-fidelity
- **Failure:** User types =1+ and presses Enter; the cell silently becomes #ERROR! (and stays committed in the model / exports as formula "1+" to xlsx, which Excel then repairs or flags). Excel keeps the user in edit mode with an error dialog.
- **Fix sketch:** Pre-validate with HyperFormula.validateFormula on commit and keep the editor open with a message on parse failure.

### IME (CJK) input cannot start a cell edit by typing
- **Where:** `src/App.tsx:1400` · editing
- **Failure:** With a Japanese IME active, select a cell and start typing: nothing happens; the user must double-click or press Enter first, and initial composed characters may be dropped.
- **Fix sketch:** Listen for compositionstart on the grid container (or keep a hidden focused input as a typing proxy) and open the editor with the composed text.

### Copying an AutoFiltered range includes filtered-out (hidden) rows
- **Where:** `src/App.tsx:1251` · excel-parity
- **Failure:** Filter a 1000-row table down to 20 visible rows, select the visible block (which spans hidden rows), copy and paste below: all 1000 rows appear, resurrecting the filtered-out data in the paste target.
- **Fix sketch:** During copy, skip rows not present in the current visible-row set (reuse getVisibleRows/hiddenRows), matching Excel's visible-cells behavior for filtered data.

### Viewport height is captured once and never updated on window/container resize — blank rows after enlarging the window
- **Where:** `src/components/Grid.tsx:162` · rendering-desync
- **Failure:** Load the app in a short window, maximize it: rows below the previous fold (beyond the 8-row overscan) render as blank spacer until the user scrolls at least one pixel.
- **Fix sketch:** Observe the scroll container with a ResizeObserver and update viewport.height.

### Switching sheets always resets scroll and selection to A1 — per-sheet view state is lost
- **Where:** `src/components/Grid.tsx:162` · excel-parity
- **Failure:** Working at row 5000 of Sheet1, flip to Sheet2 to check a value and flip back: you are dumped at A1 and must scroll back 5000 rows to resume.
- **Fix sketch:** Store selection and scroll offsets per sheetId (in component state or on the sheet model) and restore them when the sheet becomes active.

### Bar chart with many points overflows the plot area; only first 5 category labels ever shown
- **Where:** `src/components/SheetCharts.tsx:89` · ux
- **Failure:** Chart over 30 rows: only ~17 bars fit; the rest are invisible with no indication, and the point-count caption says '30 points'. Labels stop after the 5th category even when space remains.
- **Fix sketch:** Drop the 12px floor (or scroll/scale the plot), label adaptively by available width, and memoize per-chart data on [chart, formulaEngine, sheet.id].

### Name Box / Go To rejects absolute ($A$1) and sheet-qualified (Sheet2!A1) references
- **Where:** `src/lib/addressing.ts:3` · excel-fidelity
- **Failure:** User types $B$12 or Sheet2!A1 in the Name Box and gets 'Names must start with a letter...' / no navigation instead of jumping to the cell.
- **Fix sketch:** Strip $ before matching, accept an optional quoted/unquoted Sheet! prefix (switching active sheet), and support whole-row/column ranges.

### No R1C1 reference style support (display option or INDIRECT A1=FALSE mode)
- **Where:** `src/lib/addressing.ts:1` · excel-fidelity
- **Failure:** Imported workbook uses =INDIRECT("R"&ROW()&"C2",FALSE); evaluates to #REF!/#VALUE! here while Excel resolves it.
- **Fix sketch:** Document as unsupported, or add R1C1 parsing helpers plus a display toggle; INDIRECT R1C1 requires engine-side support.

### Auto-fit row height ignores wrapped text - only explicit newlines count
- **Where:** `src/lib/autoFit.ts:87` · excel-fidelity
- **Failure:** Cell with wrapText and a 300-character sentence occupies ~5 visual lines; Auto-fit row height sets 28px and the text is clipped, where Excel expands the row to show all wrapped lines.
- **Fix sketch:** When a cell in the row has wrapText, estimate lines as ceil(textWidth/columnWidth) using the same character-width constant; revisit the clamp maxima.

### Data bars: negative values get no bar and the baseline is pinned at 0 rather than range minimum
- **Where:** `src/lib/conditionalFormatting.ts:62` · excel-fidelity
- **Failure:** Data bars applied to a column of temperature deltas [-5,-3,-1]: no bars appear anywhere. Applied to [-50, 50]: -50 shows an empty cell indistinguishable from 0.
- **Fix sketch:** Scale on [min, max] over all numeric values; render negative bars with a distinct direction/color or at least proportional length.

### Boolean results display as 'true'/'false' instead of Excel's 'TRUE'/'FALSE' **[FIXED]**
- **Where:** `src/lib/formulaEngine.ts:128` · excel-fidelity
- **Failure:** A1='=1>0' displays 'true' instead of 'TRUE'; a boolean typed as TRUE round-trips display as 'true'.
- **Fix sketch:** Special-case typeof value === 'boolean' to return value ? 'TRUE' : 'FALSE'.

### getRawContent throws for unknown sheet ids while getDisplayValue returns '' — stale-sheet-id callers crash
- **Where:** `src/lib/formulaEngine.ts:35` · correctness
- **Failure:** Component memoizes a render callback with a captured sheetId, user deletes that sheet, callback runs against the new engine and getRawContent throws, breaking the render.
- **Fix sketch:** Return null from getRawContent when the sheet id is unknown, mirroring getDisplayValue's tolerance.

### Circular references display HF's #CYCLE! error, which Excel never shows
- **Where:** `src/lib/formulaEngine.ts:120` · excel-fidelity
- **Failure:** User creates an accidental circular reference; the cell shows #CYCLE! and =ISERR(A5) semantics differ from Excel, which shows 0 plus a status-bar warning.
- **Fix sketch:** Detect ErrorType.CYCLE and surface an Excel-like circular-reference warning with a 0 display, or at least document the divergence.

### COUNT renders '0' for empty row/column intersections where Excel renders blank
- **Where:** `src/lib/pivot.ts:197` · excel-fidelity
- **Failure:** Sparse Region x Product COUNT pivot: every non-occurring combination displays 0, indistinguishable from combinations that exist with empty values, and the sheet fills with noise zeros.
- **Fix sketch:** Return '' for undefined state regardless of aggregator; optionally add an 'empty cells show' option.

### Column value literally named 'Grand Total' collides with the totals column header
- **Where:** `src/lib/pivot.ts:84` · excel-fidelity
- **Failure:** A status field contains the text 'Grand Total' (common in re-imported report data); the pivot emits two 'Grand Total' columns and the sheet is ambiguous; re-selecting it as a pivot source hard-fails.
- **Fix sketch:** Suffix or quote colliding item labels (e.g. "Grand Total (item)") or rename the totals column when a collision is detected.

### MIN/MAX over a group with no numeric values renders blank where Excel shows 0
- **Where:** `src/lib/pivot.ts:204` · excel-fidelity
- **Failure:** MIN of an all-text column produces empty cells for every group instead of Excel's 0s; a report consumer diffing against Excel output sees mismatches.
- **Fix sketch:** Decide and document the convention; for strict parity return '0' for SUM/MIN/MAX when the group has records but no numeric values.

### List data validation is case-sensitive, rejecting entries Excel accepts
- **Where:** `src/lib/validation.ts:16` · excel-fidelity
- **Failure:** Validation list ['Open','Closed']; user types 'open' -> rejected with 'Choose one of: Open, Closed', while Excel accepts it.
- **Fix sketch:** Compare with toLocaleLowerCase() on both sides (consistent with the filter/CF comparisons elsewhere in the codebase).

### Sort over an auto-filtered 100k-row table allocates 6 per-cell snapshot arrays plus full-map copies, then pays the full engine rebuild
- **Where:** `src/lib/workbook.ts:1484` · performance
- **Failure:** 'Sort A to Z' from the AutoFilter menu on a 100k-row x 10-col table: several seconds in snapshot/rewrite alone, followed by the full HyperFormula rebuild and JSON.stringify — total freeze well over 10s for an operation Excel does in under a second.
- **Fix sketch:** Skip snapshotting the metadata maps that are empty, reuse a single column-address prefix per row, and sort index arrays instead of materialized row objects; the dominant cost disappears once the engine rebuild (finding 1) is fixed.

### clearSheetFilter creates an autoFilterRange on sheets that never had one
- **Where:** `src/lib/workbook.ts:914` · correctness
- **Failure:** Code path calls clearSheetFilter(wb, id, A1:C10, 1) on a sheet with no filters: the sheet acquires autoFilterRange A1:C10, and a subsequent sortRange over A1:C10 silently skips row 1 as a 'header'.
- **Fix sketch:** Only set autoFilterRange when a filter was actually removed or one already existed for the range; otherwise return sheet unchanged.

### deleteSheet can leave the active sheet pointing at a hidden sheet and allows deleting the last visible sheet
- **Where:** `src/lib/workbook.ts:1725` · ux
- **Failure:** Workbook has visible Sheet1 and hidden Sheet2. User deletes Sheet1: activeSheetId becomes the hidden Sheet2, the grid renders a sheet whose tab is not shown, and tab-based navigation has no way back to it.
- **Fix sketch:** Refuse deletion when it would leave zero visible sheets, or unhide the fallback sheet when it becomes active.

### Auto-fill numeric series detection uses strict float equality, so decimal-step series fall back to copying
- **Where:** `src/lib/workbook.ts:2190` · excel-fidelity
- **Failure:** User types 1.1 and 1.2, selects both, drags the fill handle down: cells fill 1.1, 1.2, 1.1, 1.2 instead of 1.3, 1.4.
- **Fix sketch:** Compare deltas with a relative epsilon (or compute step via least-squares like Excel) instead of ===.

